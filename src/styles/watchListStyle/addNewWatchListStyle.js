import {StyleSheet} from 'react-native';
import {ThemeColors} from '../../theme/themeColors';

export const addNewWatchListStyle = StyleSheet.create({
  addNewWatchListTextContainer: {
    marginVertical: 'auto',
  },
  addNewWatchListText: {
    color: ThemeColors.WHITE,
    fontSize: 30,
    textAlign: 'center',
    marginVertical: 10,
  },
  addNewWatchListSubText: {
    color: ThemeColors.WHITE,
    fontSize: 15,
    textAlign: 'center',
    marginVertical: 10,
  },
});
